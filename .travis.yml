language: python
sudo: false
cache: pip
env:
  global:
    - LD_PRELOAD=/lib/x86_64-linux-gnu/libSegFault.so
    - SEGFAULT_SIGNALS=all
  matrix:
    - TOXENV=check
    - TOXENV=docs
matrix:
  include:

    - python: '2.7'
      env:
        - TOXENV=py27-cover,report,coveralls,codecov
    - python: '2.7'
      env:
        - TOXENV=py27-nocov
    - python: '3.3'
      env:
        - TOXENV=py33-cover,report,coveralls,codecov
    - python: '3.3'
      env:
        - TOXENV=py33-nocov
    - python: '3.4'
      env:
        - TOXENV=py34-cover,report,coveralls,codecov
    - python: '3.4'
      env:
        - TOXENV=py34-nocov
    - python: '3.5'
      env:
        - TOXENV=py35-cover,report,coveralls,codecov
    - python: '3.5'
      env:
        - TOXENV=py35-nocov
    - python: '3.6'
      env:
        - TOXENV=py36-cover,report,coveralls,codecov
    - python: '3.6'
      env:
        - TOXENV=py36-nocov
    - python: 'pypy'
      env:
        - TOXENV=pypy-cover,report,coveralls,codecov
    - python: 'pypy'
      env:
        - TOXENV=pypy-nocov
before_install:
  - python --version
  - uname -a
  - lsb_release -a
install:
  - pip install tox
  - virtualenv --version
  - easy_install --version
  - pip --version
  - tox --version
script:
  - tox -v
after_failure:
  - more .tox/log/* | cat
  - more .tox/*/log/* | cat
notifications:
  email:
    on_success: never
    on_failure: always
